# Runtime dependencies, pinned to the versions the test suite passes with.
# Update deliberately: bump a version, run `pytest`, and commit the change.
fastapi==0.141.1
uvicorn==0.52.4
pydantic==2.13.4
PyYAML==6.0.3
httpx==0.28.1
Jinja2==3.1.6
python-dotenv==1.2.3

# Transitive dependencies (pinned so installs are reproducible)
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.14.2
certifi==2026.7.22
click==8.5.0
h11==0.16.0
httpcore==1.0.9
idna==3.19
MarkupSafe==3.0.3
pydantic_core==2.46.4
starlette==1.6.0
typing-inspection==0.4.4
typing_extensions==4.16.0
